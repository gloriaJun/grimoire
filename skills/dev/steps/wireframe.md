# Wireframe (Deprecated)

> **This step has been removed.** Wireframe generation is now part of the `design` step.
>
> See `steps/design.md` — Section 3: "Wireframe generation".

If you arrived here from a legacy task with `currentStep: "wireframe"`:
1. Migrate the task to a memory file (see `schemas/state.md` Migration section)
2. Set `current-step` to `"build"` (design is considered complete)
3. Continue with `/dev build`
